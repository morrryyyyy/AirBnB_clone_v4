$('document').ready(function(){
  let amenities = {}
  $('input[type="checkbox"]').change(function(){
    if amenities[$(this).attr('data_id')] = $(this).attr('data_name');
    ekse {
      delete amenities[$(this).attr('data_id')];
    }
    }
  })
})
